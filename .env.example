# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# SSH Key Path (for git clone operations)
# Default: ~/.ssh
SSH_KEY_PATH=/path/to/.ssh

# Claude Code CLI Configuration Path
# Default: ~/.config/claude
CLAUDE_CONFIG_PATH=/path/to/.config/claude

# Git Configuration Path (optional)
# Default: ~/.gitconfig
GIT_CONFIG_PATH=/path/to/.gitconfig
